# Hybrid Classifier

A simple binary quantum computing classifier.

## Setup

1. Activate the virtual environment:
   ```bash
   # On Windows:
   .\venv\Scripts\activate
   # On Unix or MacOS:
   source venv/bin/activate
   ```
2. Install dependencies (to be added)
